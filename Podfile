# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'kompas.id-submission' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for kompas.id-submission
  pod 'Alamofire', '~> 5.2'
  pod 'RxSwift', '6.2.0'
  pod 'RxCocoa', '6.2.0'

  target 'kompas.id-submissionTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'kompas.id-submissionUITests' do
    # Pods for testing
  end

end
